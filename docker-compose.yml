version: "3"
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: docker-react-frontend
    ports:
      - "3000:80"
    volumes:
      - /app/node_modules
      - .:/app
